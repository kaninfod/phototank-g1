.box-comment
  %img.img-circle.img-sm{:alt => "User Image", :src => comment.user.avatar.url}
    .comment-text
      %span.username
        =comment.user.name
        %span.text-muted.pull-right=time_ago_in_words(comment.created_at)
      =comment.comment
