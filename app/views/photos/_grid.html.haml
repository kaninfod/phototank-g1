- if @photos.blank?
  No Photos to display
- else
  = render @photos
  .hide
    =will_paginate @photos,  :page_links=>false

=render "layouts/bucket_panel"


#photo-modal.fullscreen-modal.modal
  .modal-content
